name: hair_salon
description: A new Flutter project for a hair salon app.

publish_to: 'none' # Remove this line if you want to publish your app to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/hairstyle.png # Add your asset paths here, if any.
